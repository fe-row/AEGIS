# ═══════════════════════════════════════════════════════
#  AEGIS — .env Template for New Deployments
#  Copy this file to .env and fill in your values.
# ═══════════════════════════════════════════════════════

# ── Database ──
DB_PASSWORD=CHANGE_ME_use_a_64_char_random_string

# ── Redis ──
REDIS_PASSWORD=CHANGE_ME_use_a_44_char_random_string

# ── JWT signing ──
# Default: RS256 (asymmetric). Generate keys: python scripts/generate_rsa_keys.py
JWT_ALGORITHM=RS256
JWT_PRIVATE_KEY="CHANGE_ME_paste_pem_private_key_with_escaped_newlines"
JWT_PUBLIC_KEY="CHANGE_ME_paste_pem_public_key_with_escaped_newlines"
# Fallback for HS256 (symmetric, simpler but less secure):
# JWT_ALGORITHM=HS256
JWT_SECRET=CHANGE_ME_use_a_86_char_random_string

# ── Fernet encryption (run: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") ──
ENCRYPTION_KEY=CHANGE_ME_generate_fernet_key

# ── Webhooks ──
SLACK_WEBHOOK_URL=
TEAMS_WEBHOOK_URL=
HITL_WEBHOOK_URL=

# ── CORS (comma-separated origins) ──
CORS_ORIGINS=https://your-domain.com

# ── Environment ──
ENVIRONMENT=production

# ── Domain (for TLS / Certbot / Nginx) ──
DOMAIN=your-domain.com

# ── Grafana ──
GRAFANA_PASSWORD=CHANGE_ME_use_a_strong_password

# ── Backend tuning (optional) ──
WORKERS=4
LOG_LEVEL=info
MAX_CONCURRENT=200
MAX_REQUESTS=10000

# ── Database Pool (optional) ──
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# ── Scheduler (optional) ──
AUDIT_FLUSH_INTERVAL_SECONDS=10

# ═══════════════════════════════════════════════════════
#  Enterprise Features (v5.0)
# ═══════════════════════════════════════════════════════

# ── SSO / OpenID Connect ──
SSO_ENABLED=false
SSO_PROVIDER=                    # okta | azure_ad | google
SSO_CLIENT_ID=
SSO_CLIENT_SECRET=
SSO_DISCOVERY_URL=               # e.g. https://login.microsoftonline.com/{tenant}/v2.0/.well-known/openid-configuration
SSO_REDIRECT_URI=                # e.g. https://your-domain.com/api/v1/auth/sso/callback

# ── OpenTelemetry Distributed Tracing ──
OTEL_ENABLED=false
OTEL_EXPORTER_ENDPOINT=http://jaeger:4317
OTEL_SERVICE_NAME=aegis-backend

# ── Alerting (PagerDuty / OpsGenie) ──
ALERT_PROVIDER=                  # pagerduty | opsgenie | pagerduty,opsgenie
PAGERDUTY_ROUTING_KEY=
OPSGENIE_API_KEY=

# ── Secrets Management ──
SECRETS_PROVIDER=env             # env | vault | aws
VAULT_ADDR=                      # e.g. http://127.0.0.1:8200
VAULT_TOKEN=
VAULT_SECRET_PATH=secret
AWS_SECRET_NAME=aegis/production

# ── Redis HA (Sentinel) ──
# REDIS_SENTINEL_HOSTS=           # e.g. sentinel1:26379,sentinel2:26379,sentinel3:26379
# REDIS_SENTINEL_MASTER=mymaster